apiVersion: v1
kind: Pod
metadata:
  name: hello-world
  labels:
    app: hello-world
spec:
  containers:
  - name: hello-world
    image: kordiandziwisz/education_kubernetes_hello_world:latest
    ports:
    - containerPort: 8080
    # i need to label this as "backend"